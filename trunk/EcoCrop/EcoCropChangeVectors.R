#This R script calculates the change vectors and metrics for a specified set of previously calculated suitability runs
#You must have at least the baseline and one possible future (given by a downscaled GCM output)

EcoCropChangeVectors <- function(baseline='', nsc=7, future='', cropname='') {
	
}