dDir <- "C:/Users/Stephanie/Desktop/CIAT/inputs/empty"

a <- list.files(dDir)
if a=="character(0)"



this_sppName2 <- "example.txt"

file.exists(this_sppName2)
  
  {
  setwd(file.path(mainDir, subDir))
} else {
  dir.create(file.path(mainDir, subDir))
  setwd(file.path(mainDir, subDir))
  
}
